const formattedDate = formatDate(new Date())
// formatDate is declared in global name space. Thus there is no need to import/ export this methods
// hover on the method, you can see the comments as well
console.log(formattedDate)